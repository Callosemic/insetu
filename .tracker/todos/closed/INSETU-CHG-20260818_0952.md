---
id: "INSETU-CHG-20260818_0952"
repo: "insetu"
type: "todo"
status: "closed"
title: "Standardize vfs:// Logical Path Resolution, Yomama Bridge Boundary Mapping, and Purge Legacy Path Heuristics"
sub_bucket: "None"
created_at: "0000-00-00T00:00:00"
closed_at: "2026-08-18T09:53:21"
---

## Description
* **Yomama Bridge vfs:// Prefix Normalization (`insetu/core/bridge/bridge_vfs.py`)**: Standardized `_process_sync_transaction` to normalize target file paths starting with `vfs://` logical URI scheme prefixes.
* **VFS Route Sandbox Path Resolution (`insetu/core/routes_fs.py`)**: Updated `resolve_vfs_file` to evaluate `check_name` without `vfs://` prefixes before determining artifact classification and sandbox path resolution.
* **Logical Path Resolution Standardization (`insetu/core/utils_core.py`)**:
  * Unified `vfs://` URI scheme handling inside `resolve_logical_path` and removed duplicate string stripping from `hook_vfs_resolve_path`.
  * Purged legacy `::` repository boundary syntax parsing from `resolve_logical_path`.
  * Removed dangerous path fallback heuristics that stripped leading `repo_dir` segments during file creation, enforcing exact path relative to workspace root to prevent ghost package boundaries.