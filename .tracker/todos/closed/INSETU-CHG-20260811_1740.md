---
repo: "insetu"
type: "todo"
status: "closed"
id: "INSETU-CHG-20260811_1740"
title: "Global Master Encryption Key Migration and Backend Settings Error Handling"
created_at: 0000-00-00T00:00:00
closed_at: 2026-08-11T17:40:00
sub_bucket: "None"
tags: ["auth", "security", "settings", "changelog"]
---

## Description
* **Global Encryption Key Resolution & Migration (`insetu/kernel/auth.py`)**: Updated `get_master_key()` to store and read the Fernet master encryption key globally at `~/.insetu/master_key.txt`. Implemented safe migration for legacy workspace-local `.insetu/master_key.txt` files (moving them to the global user path if no global key exists, or preserving local keys if both exist) to prevent secrets decryption failures across workspace relocations.
* **Master Key Settings Configuration (`insetu/kernel/auth.py`)**: Updated `security_bp` settings description and `update_security_settings()` endpoint to target the active key path (local legacy key if present, otherwise global home path).
* **Backend Settings Save Error Handling (`insetu/kernel/extension.py`)**: Wrapped the `InSetuExtension.update_settings` endpoint in a defensive `try...except` block to log execution tracebacks and return structured JSON error payloads (`status: "error"`, status code 500) upon save failures.
* **Default Gitignore Hardening (`insetu/defaults/gitignore.txt`)**: Added `master_key.txt` to default `.gitignore` templates to prevent accidental commits of encryption keys.

## Notes / Execution Log
* Verified 100% compliance against static fitness functions and system guardrails.