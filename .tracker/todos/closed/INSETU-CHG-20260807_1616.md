---
repo: "insetu"
type: "todo"
status: "closed"
id: INSETU-CHG-20260807_1616
title: "Git Subprocess Centralization, Baseline Auto-Tagging, and Reactive Update Tracking"
created_at: 0000-00-00T00:00:00
closed_at: 2026-08-07T16:18:50
sub_bucket: "None"
---

## Description
* **Centralized Git Execution Wrapper (`engine_git.py`, `engine_update.py`)**: Wrapped git command invocations using `execute_git()`, ensuring `--no-optional-locks` and headless SSH injection (`GIT_SSH_COMMAND`) across Git and Semantic Update backend engines.
* **Semantic Release Baseline Auto-Tagging (`engine_update.py`)**: Added explicit `v{initial_version}` Git tag creation during scaffolding (`_background_scaffold_task`) and TOML generation (`_background_create_dummy_toml`) to establish the baseline tag for version calculations.
* **Conventional Commits Schema Alignment (`engine_update.py`)**: Standardized `UPDATE_SETTINGS_SCHEMA` commit parser default to `"conventional"`.
* **Output Log Sanitization (`engine_update.py`)**: Cleaned raw timestamp and file reference noise from release preview logs.
* **Reactive VFS Mutation & Diff Event Dispatch (`ext_update.js`)**: Registered `zone:vfs-mutated` UI hook with debounced repo status re-fetching and emitted `insetu:git:generate-diffs` on version initialization.

## Notes / Execution Log
* Verified compliance against static fitness functions (`tests/fitness_functions.py`).