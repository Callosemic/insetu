---
repo: "insetu"
type: "todo"
status: "closed"
id: INSETU-CHG-20260810_1436
title: "Encapsulate Core System Settings and Introduce Editor Preferences Extension"
created_at: 0000-00-00T00:00:00
closed_at: 2026-08-10T14:38:46
sub_bucket: "None"
---

## Description
* **Core System Settings Encapsulation (`insetu/core/routes_system.py`)**:
  * Registered `core_system` settings schema (`_REGISTERED_SETTINGS_SCHEMAS['core_system']`) declaring `port`, `enable_watchdog`, `preload_all_extensions`, `instance_title`, and `instance_emoji`.
  * Instantiated `core_system_ext` using `InSetuExtension`.
  * Standardized workspace configuration index references from `workspaces.json` to `system.json` across workspace creation (`api_create_workspace`), deletion (`api_delete_workspace`), and retrieval (`api_workspaces`) endpoints.
  * Seeded workspace `instance_title` via `SettingsManager('core_system', ws_id)` during workspace initialization.
* **Editor Preferences Core Extension (`insetu/core/editor/engine_editor.py`)**:
  * Created new core extension `editor` (`InSetuExtension`) registering global editor preferences (`EDITOR_SCHEMA`) with `insetu_md_links` setting.

## Notes / Execution Log
* Fully verified against static fitness functions and system guardrails.