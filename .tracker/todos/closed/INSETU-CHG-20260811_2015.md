---
repo: "insetu"
type: "todo"
status: "closed"
id: "INSETU-CHG-20260811_2015"
title: "Semantic Update Extension DRY Log Cleaning, Initial Release Preview, and Sutram UI Control Modernization"
created_at: 0000-00-00T00:00:00
closed_at: 2026-08-11T20:15:00
sub_bucket: "extensions"
tags: ["changelog", "update", "sutram", "preview", "vfs"]
---

## Description
* **DRY Log Sanitization (`engine_update.py`)**: Abstracted log timestamp and file reference cleaning into `_clean_semantic_release_logs` across preview and bump background worker tasks.
* **Initial Release Dry-Run Preview (`engine_update.py`, `ext_update.js`)**: Added `preview_first_release_task` worker and `preview_first_release` endpoint allowing developers to preview initial release plans prior to PyPI distribution.
* **VFS Relative Path Normalization (`engine_update.py`)**: Standardized `pyproject.toml` read and save operations to consume relative workspace paths via `ctx.vfs.read()` and `ctx.vfs.save()`.
* **Sutram Form Control Modernization (`ext_update.js`)**: Replaced raw HTML `<select>` and `<input>` elements in `InSetuExtUpdate` with `<sutram-select>` and `<sutram-input>` primitives.
* **Client SDK Job & Debounce Alignment (`ext_update.js`)**: Abstracted preview job polling via `_runPreviewJob`, unified release job bindings in `_getReleaseAction`, and migrated VFS mutation debouncing to `ExtensionRegistry.utils.debounce`.

## Notes / Execution Log
* Verified 100% compliance against static fitness functions (`tests/fitness_functions.py`).