---
id: "INSETU-CHG-20260831_1725"
title: "Semantic Update GitHub Token Support, Twine Upload Standardization, and UI Action Enablement Fixes"
repo: "insetu"
type: "epic"
status: "closed"
sub_bucket: "extensions"
created_at: "0000-00-00T00:00:00"
closed_at: "2026-08-31T17:25:46"
---

## Description
* **GitHub Personal Access Token Integration (`engine_update.py`)**: Added `github_token` (`password`, `secure: True`, `scope: "workspace"`) to `UPDATE_SETTINGS_SCHEMA` and injected `GH_TOKEN` into the environment during `semantic-release publish` worker execution.
* **Working Tree & Path Validation Standardization (`engine_update.py`)**: Introduced `_get_valid_repo_path` and `_require_clean_tree` helper functions to standardize path validation and pre-flight working tree cleanliness checks across bump, publish, and first-release tasks.
* **Twine Upload Consolidation (`engine_update.py`)**: Centralized PyPI package upload execution into `_execute_twine_upload`, removing duplicate upload logic across initial release and publish worker tasks.
* **Publish & Release Action Enablement (`ext_update.js`)**: Refined `step2Disabled` and `isFirstReleasePhase` evaluation logic in `InSetuExtUpdate` to ensure initial release actions accurately evaluate working tree cleanliness without being incorrectly blocked by post-release state checks.
* **Release Log State Preservation (`ext_update.js`)**: Standardized `lastReleaseLog` store updates across job completion and error callbacks in `InSetuExtUpdate` and `_getReleaseAction`.

## Notes / Execution Log
* Executed fitness functions validator (`tests/fitness_functions.py`), confirming 100% compliance across all Python, JavaScript, and CSS architectural rules.