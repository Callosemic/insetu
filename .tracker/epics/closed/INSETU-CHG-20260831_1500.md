---
id: "INSETU-CHG-20260831_1500"
title: "Sync Bridge Revert Snapshot Fix, Worker Scheduling Refactor, and Path Resolution Hardening"
repo: "insetu"
type: "epic"
status: "closed"
created_at: "0000-00-00T00:00:00"
closed_at: "2026-08-31T16:24:56"
sub_bucket: "None"
---

## Description
* **Sync Bridge Atomic Revert Snapshot Resolution (`engine_bridge.py`):** Fixed snapshot query operator in `bridge_revert` to evaluate `<=` against transaction timestamps, ensuring snapshots created at the transaction boundary itself are correctly identified during initial state restorations.
* **Gather Delayed Job Worker Refactor (`engine_gather.py`):** Replaced direct SQLite `jobs` table insertions and deletions with the canonical `submit_one_shot_job` API from `insetu.kernel.workers` to ensure automatic job eviction and prevent infinite metronome loops.
* **Gather Empty Bucket Callback Normalization (`engine_gather.py`):** Normalized generator callback return dictionary structure for empty file lists to return `{"header": "", "blocks": [], "files": []}` rather than `None`.
* **Differential Context Event Propagation (`engine_gather.py`):** Fixed `_surgically_update_manifest` to preserve and pass incoming `ledger_events` through `_background_compile` so surgical differential updates retain event context.
* **External Repository Logical Path Resolution (`utils_core.py`):** Fixed `resolve_logical_path` for repositories configured with explicit `physical_path` attributes, ensuring new file creation operations resolve directly to the external repository directory rather than falling back to the workspace root.

## Notes / Execution Log
* Executed full fitness function test suite (`tests/fitness_functions.py`), confirming 100% compliance across all Python, JavaScript, and CSS architectural rules.