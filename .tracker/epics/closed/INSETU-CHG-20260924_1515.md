---
id: "INSETU-CHG-20260924_1515"
title: "Emergency Context Zip Archiving, OpenAPI Token Gate Bypass, and Fitness Linter Expansion"
repo: "insetu"
type: "epic"
status: "closed"
tier: 2
tags:
  - app
  - fallback_bridge
  - openapi
  - fitness
  - linter
  - changelog
created_at: '2026-09-24T15:15:00'
closed_at: '2026-09-24T15:15:00'
sub_bucket: "None"
---

## Description
* **Token Gate & OpenAPI Bypass (`insetu/app.py`)**: Whitelisted `/api/system/openapi.json` in `enforce_token_gate()` to allow unauthenticated OpenAPI 3.0 spec discovery for collaborating LLM tools.
* **Werkzeug Logging Filter (`insetu/app.py`)**: Added `QuietPollingFilter` in `run_app()` to filter out noisy terminal logging from high-frequency polling and delta endpoints (`/system/deltas`, `/system/jobs/`, `/gather/manifest/entry`, `/git/sweep/status`).
* **Default Configuration Normalization (`insetu/defaults/config_json.txt`)**: Renamed legacy `"targets": []` key to canonical `"target_repos": []` in default workspace scaffolding.
* **Emergency Recovery OS Zip Archiving (`insetu/fallback_bridge.py`)**: Upgraded `/api/system/emergency_dump` to generate a multi-bucket ZIP archive (`emergency_core_context.zip`) with categorized context files (`00_FILE_STRUCTURE.txt`, `akasa.txt`, `vendor.txt`, `extensions.txt`, `core.txt`, `misc.txt`) while stripping base64 source maps from vendor JavaScript to conserve token budgets. Updated reboot redirect URL to `/`.
* **Architectural Fitness Linter Expansion (`tests/fitness/`)**: Added `top_level_imports` tracking to `rules_python.py` (`REDUNDANT_INLINE_IMPORT` rule) to catch duplicate inline imports inside functions. Added `engine_notes.py` to `VFS_WRITE_WHITELIST` and defined `EXECUTE_BINARY_WHITELIST` in `core.py`.
* **Repository Code Index Alignment (`CODE_INDEX.md`)**: Updated topological descriptions for ADR 0050, 0051, and 0052, and removed duplicate active entries for archived ADRs 0010 and 0011.

## Notes / Execution Log
* Executed full architectural audit against Engineering Standard 01; verified zero breaking changes introduced.