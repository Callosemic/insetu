---
id: INSETU-CHG-20260907_1007
title: "Hybrid AST JavaScript Fitness Linter, Ruamel YAML Integration, Dual-Root Manifest Partitioning, and Kernel Utilities Expansion"
repo: insetu
type: todo
status: closed
created_at: '2026-09-07T10:07:00'
closed_at: '2026-09-07T10:07:00'
sub_bucket: None
tier: '3'
tags: []
delivery_date: null
parent_id: null
depends_on: []
---

## Description
* **Hybrid AST JavaScript Fitness Linter (`rules_javascript.py`):** Integrated `tree-sitter-javascript` AST parsing into the frontend fitness functions validator for structural checks on UDF state bleed, custom event dispatches, approved API verb chains, and `clearTimeout` debounce mandates, with graceful fallback to regex analysis.
* **Ruamel YAML Frontmatter Engine & Dependencies (`pyproject.toml`, `utils_core.py`):** Integrated `ruamel.yaml` into core dependencies for round-trip YAML frontmatter parsing and added `syntax` optional dependencies (`tree-sitter`, `tree-sitter-python`, `tree-sitter-javascript`).
* **Dual-Root Partitioned Manifest & Kernel URI Utilities (`extension.py`, `utils.py`):** Refactored `ExtensionContext.manifest` to yield partitioned `{"vfs": vfs_manifest, "ctx": ctx_manifest}` payloads and exposed `parse_uri()` as a Tier 1 kernel helper and `ExtensionContext` method.
* **Kernel Worker Job Cancellation (`workers.py`):** Added `cancel_job()` helper to `insetu/kernel/workers.py` for programmatic cancellation of scheduled background tasks in the worker ledger.
* **Client App Shell & Offline Gateway Polish (`app.js`):** Cleaned up duplicate metronome tick registrations, added `AbortSignal.timeout()` timeout guards, pinned job polling requests to the originating workspace scope, and unified dual-root manifest handling across hydration routines.
* **js-yaml Vendorization (`vendor.json`, `vendor_jsyaml.sh`):** Added `js-yaml` ESM mapping to `vendor.json` and added `vendor_jsyaml.sh` esbuild vendorization script.

## Notes / Execution Log
* Verified 100% compliance across all static analysis rules and fitness functions.