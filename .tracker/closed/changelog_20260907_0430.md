---
id: INSETU-CHG-20260907_0430
title: "Cronic Job Manager Extension, Dev SQL Console, Notes IoC RAG Topology, and Tracker Hardware Acceleration"
type: "epic"
status: "closed"
repo: "insetu"
tier: 3
---

## Description
* **Cronic Job Manager Extension (`engine_cronic.py`, `ext_cronic.js`)**: Added a new system task scheduling extension supporting Unix `crontab` management, manual script executions (`run_now`), process termination (`kill_job`), background log rotation (`sweep_logs_task`), and workspace boot drift healing.
* **Developer Extension SQL Console (`engine_dev.py`, `ext_dev.js`)**: Added `/sql/databases` and `/sql/query` REST endpoints and `<insetu-ext-dev-sql>` Lit component for direct, constrained SQL execution against workspace SQLite databases. Standardized log copying via `this.utils.copyRawText()`.
* **Notes Extension IoC Topology (`engine_notes.py`, `ext_notes.js`)**: Replaced deprecated `inject_notes_context` hook with IoC `declare_notes_topology` via `@hooks.on('gather_declare_topology')` (ADR 0037 compliance). Added `coalescedAsync` fetching, optimistic VFS mutation handling, and offline cache pre-fetching.
* **Tracker Extension Hardware Acceleration & Metadata Restoration (`engine_tracker.py`, `ext_tracker.js`)**: Added hardware-accelerated SQL query indexes (`idx_tracker_*`) and SQLite C-extension JSON1 aggregation (`json_group_array`/`json_object`) for `/files` and `/index` endpoints. Added `/restore_metadata` background worker to recover wiped frontmatter dates from Git history.
* **Favorites, Flow, Format, Git, Ingest, Prompts, Research, Skills, Term Hardening**: Standardized API request option collapsing (`collapseKey`), `coalescedAsync` store fetching, `vfs://` URI boundary handling, `ruamel.yaml` frontmatter updates, and touch gesture/clipboard controls.

## Notes / Execution Log
* Verified 100% compliance across all diff payloads.
* Applied linter regex fix in `tests/fitness/rules_javascript.py` to eliminate false-positive API verb flags.