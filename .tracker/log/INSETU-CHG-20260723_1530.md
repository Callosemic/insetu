---
repo: "insetu"
type: "todo"
status: "logged"
id: INSETU-CHG-20260723_1530
title: "Modal Fullscreen Standardization, Dynamic Manifest Hydration, and Workflow Diff Event Propagation"
created_at: 0000-00-00T00:00:00
closed_at: 2026-07-23T15:34:43
sub_bucket: "None"
---

## Description
* **Modal Fullscreen Standardization & Body Layout Hardening**: Standardized the use of declarative boolean `?fullscreen=${true}` properties and flexbox scroll body containers (`flex: 1; min-height: 0; overflow-y: auto;`) across `<insetu-modal>` instances in `ext_citations.js`, `ext_format.js`, `ext_git.js`, `ext_hooks.js`, `ext_research.js`, `ext_skills.js`, and `ext_tracker.js`.
* **Workflow Diff Event Propagation (`engine_git.py` & `ext_flow.js`)**: Updated `_background_generate_diffs` in `engine_git.py` to emit the `compile_contexts` lifecycle hook when diff files are generated. Bound a `git-diffs-refreshed` event listener in `InSetuExtFlow` (`ext_flow.js`) to automatically re-fetch workflow batches when background diff generation completes.
* **JIT Manifest State Rehydration (`ext_git.js`)**: Refactored `generateDiffs` in `ext_git.js` to replace brittle, imperative in-memory manifest mutation with an explicit REST fetch to `/api/<workspace>/manifest`, ensuring downstream UI views immediately receive updated batch chunks and diff metadata.

## Notes / Execution Log
* Verified against static fitness functions and system guardrails. All extension components maintain 100% compliance.