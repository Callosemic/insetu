---
repo: insetu
type: epic
status: logged
id: INSETU-CHG-20260812_1025
title: INSETU-CHG-20260812_1025
created_at: '2026-08-22T20:31:44-07:00'
closed_at: '2026-09-04T10:34:59'
sub_bucket: None
tier: 1
---

## Description
* **Yomama Sync Bridge Candidate Confirmation State Persistence (`insetu/static/js/core/bridge.js`)**:
  * Reset `_confirmedCandidates` instance state on `InSetuExtBridge` when `BridgeStore.clearPayload()` is invoked.
  * Passed `confirmed_candidates` dictionary in `_getSyncAction()` payload getter to preserve user-selected candidate path overrides during background job dispatch.
  * Updated `_handleConsoleClick()` for candidate confirmation and remapping actions to record selections in `_confirmedCandidates` state and trigger live sync re-runs.
  * Refined telemetry card action button labels to display "Confirm Overwrite" when candidate match type is `overwrite` or carries `confirm-to-overwrite` flags.
* **Global Allowed Extensions Input Sanitization (`insetu/static/js/core/config.js`)**:
  * Refined `InSetuExtConfig` input array filtering for `include_extensions` to filter out `null` and `undefined` entries while preserving valid strings.

## Notes / Execution Log
* Analyzed diff payload in `insetu/static/js/core/bridge.js` and `insetu/static/js/core/config.js`.
* Verified 100% compliance with static fitness functions (`tests/fitness_functions.py`) and system guardrails.