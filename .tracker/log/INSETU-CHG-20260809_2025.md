---
repo: "insetu"
type: "todo"
status: "logged"
id: INSETU-CHG-20260809_2025
title: "Yomama Bridge Candidate Confirmation Payload Alignment and VFS Explorer Reactive Manifest Refresh"
created_at: "2026-08-09T20:25:00"
closed_at: "2026-08-09T20:25:00"
sub_bucket: "None"
tags: ["changelog", "bridge", "vfs", "sdk", "ui"]
---

## Description
* **Yomama Sync Bridge Candidate Path & Action Overrides (`bridge.js`)**:
  * Unified `force` boolean flag propagation in `_getSyncAction` across manual overrides and candidate confirmations.
  * Enhanced `_handleConsoleClick` handlers (`confirm-candidate`, `force-sync`, `ignore-syntax`, `deep-search`) to explicitly pass `force: true` and candidate mapping dictionaries (`confirmed_candidates` / `confirmed_overwrites`) to the backend Sync Bridge job.
  * Rendered top-level telemetry message alerts (`t.message`) inside `_renderTelemetry()`.
  * Updated dry-run completion controls to expose the `⚡ Apply Patch` button with pre-populated `confirmed_candidates` when `can_commit` is true.
* **VFS Explorer Reactive Manifest Refresh (`fs.js`)**:
  * Implemented `onForceRefresh()` and `onWorkspaceChanged()` lifecycle handlers on `InSetuVFSExplorer` to trigger `window.inSetu.sys.refreshManifest()` with visual `<insetu-spinner>` loading feedback.
  * Registered `zone:subtab-changed` UI hook in the `files` extension definition to trigger `refreshManifest()` automatically when navigating to the `edit` -> `files` sub-tab.

## Notes / Execution Log
* Verified 100% compliance with static fitness functions (`tests/fitness_functions.py`) and engineering standards.