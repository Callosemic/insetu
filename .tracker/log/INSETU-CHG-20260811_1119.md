---
repo: "insetu"
type: "todo"
status: "logged"
id: INSETU-CHG-20260811_1119
title: "Client SDK API Modernization, Entity Action Decomposition, and Stage 1 Manifest Isolation"
created_at: 0000-00-00T00:00:00
closed_at: 2026-08-11T11:19:12
sub_bucket: "None"
---

## Description
* **Client SDK Type Contracts & JSDoc Definitions (`types.js`, `sdk.js`)**: Created `insetu/static/js/core/types.js` establishing the canonical JSDoc `@typedef {Object} EntityData` contract (`filepath`, `repoDir`, `titleText`, `entityType`, `isFS`). Annotated `InSetuElement.ecosystem` and `window.inSetu.utils.normalizeEntityData` with JSDoc types.
* **Entity Action Decomposition & Direct Downloads (`fs.js`, `gather.js`)**: Decomposed `file-download` entity action into a dedicated `file-view-parts` entity action (`order: 105`) for multi-part contexts. Updated `file-download` (`order: 100`) and Gather `batch-download` to download multi-part context and quickpack chunks directly off-thread.
* **API Gateway Standardization & Telemetry Cleanup (`bridge.js`)**: Standardized Yomama Sync Bridge history actions (`bridge/revert`) to consume `this.api.post()`. Cleaned up redundant force flags and telemetry renderings.
* **Stage 1 Manifest Isolation & VFS Tree Filtering (`fs.js`)**: Isolated optimistic `updateManifestState` updates to Stage 1 physical `vfs` manifest partitions and filtered `ctx` files out of `InSetuVFSExplorer` and `getGlobalManifest()` to prevent context payloads from cluttering the VFS file tree.
* **Declarative Keyboard Shortcuts (`config.js`, `ui_editor.js`, `fs.js`)**: Registered keyboard shortcuts (`ctrl+s`, `escape`, `tab`) within the `config`, `editor`, and `files` extension registrations.
* **Editor Dirty-State Event Dispatching (`ui_editor.js`)**: Dispatched `'editor-dirty'` CustomEvents during frontmatter editor load, check, and save cycles for reactive UI sync.

## Notes / Execution Log
* Verified 100% compliance against static fitness functions (`tests/fitness_functions.py`).