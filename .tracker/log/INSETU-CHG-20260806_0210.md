---
repo: "insetu"
type: "todo"
status: "logged"
id: INSETU-CHG-20260806_0210
title: "Decentralized Manifest Signature Sync Kernel Substrate, Sutram Form Control Linter Guardrail, and Extension Observer Decoupling"
created_at: 0000-00-00T00:00:00
closed_at: 2026-08-06T02:09:57
sub_bucket: "None"
---

## Description
* **Decentralized Manifest Sync Kernel Substrate (`insetu/kernel/sync.py`)**: Created Tier 1 kernel synchronization module implementing `get_system_deltas()`. Aggregates physical file events from `vfs_event_log`, determines active compilation status from job ledgers, and collects domain state signatures via `@hooks.on('register_manifest_signatures')`.
* **Frontend Heartbeat & Signature Router (`insetu/static/js/app.js`)**: Refactored the client background metronome (`manifest_sync`) to query `/api/system/deltas`, evaluating `sigs.vfs` and `sigs.ctx` state signatures to perform surgical, single-entry fetches (`topology/vfs` and `gather/manifest/entry`) and optimistic Zustand `AppStore` manifest splicing.
* **Werkzeug Reload Boot Guardrail (`insetu/app.py`)**: Wrapped `system_boot` event emission in `WERKZEUG_RUN_MAIN` environment check to prevent duplicate background worker initialization during development reloads.
* **Kernel Worker & Observer Decoupling (`insetu/kernel/workers.py`)**: Extracted `NonGitDirectoryWatcher` out of `workers.py` to `insetu.core.utils_core`, added SQL performance indexes (`idx_vfs_event_log_ts`, `idx_immediate_jobs_status`, `idx_jobs_status`), and simplified `start_workers()` to delegate watcher startup to core utilities.
* **Virtual URI Scheme Cleanup (`insetu/kernel/extension.py`, `insetu/kernel/vfs.py`)**: Removed legacy `system://` URI checks from `ExtensionContext.expand_selection()` and `VFSTransaction.read()`, completing the migration to `ctx://`.
* **Sutram Form Control Fitness Linter Rule (`tests/fitness/rules_javascript.py`, `tests/fitness/core.py`)**: Added `SUTRAM_FORM_CONTROL_MANDATE` checking for raw HTML `<input>`, `<select>`, and `<textarea>` elements in Lit extension templates, mandating `<sutram-input>`, `<sutram-select>`, `<sutram-toggle>`, or `<sutram-textarea>` controls. Added `engine_tailscale.py` to subprocess whitelist in `tests/fitness/core.py`.
* **Architectural Map Synchronization (`CODE_INDEX.md`)**: Updated `CODE_INDEX.md` to record `ADR 0039`, `2026-08-06-decentralized-manifest-sync.md` standup blueprint, and `insetu/kernel/sync.py`.