---
repo: "insetu"
type: "todo"
status: "logged"
id: INSETU-CHG-20260728_2355
title: "Gather Engine Worker Encapsulation, Manifest Filtering, and yenVUI Component Alignment"
created_at: 0000-00-00T00:00:00
closed_at: 2026-07-28T23:54:36
sub_bucket: "None"
---

## Description
* **Gather Engine Background Compilation Worker Encapsulation (`engine_gather.py`)**: Relocated `get_compiler_lock`, `_background_compile` worker task, and `api_gather_submit` route from `app.py` directly into `engine_gather.py` to complete Tier 2 Gather engine extraction (ADR 0029).
* **Manifest Extraction Type Filtering (`utils_core.py`)**: Enhanced `extract_manifest_files` and `get_available_contexts` to accept optional `include_types` and `exclude_types` parameters, allowing surgical manifest chunk filtering without context recursion.
* **Vendor Shared Styles Alignment (`bridge.js` & `gather.js`)**: Updated relative `sharedStyles` imports across Tier 2 core JS modules to point to `../../vendor/sutram/shared_styles.js`.
* **yenVUI Gather Modal Migration (`gather.js`)**: Replaced legacy `<insetu-modal>` and `@modal-closed` listener in `InSetuExtGather` with `<yenvui-modal>` and `@yenvui-modal-closed`. Removed top-level `DOMContentLoaded` event listener wrapper around extension registration.